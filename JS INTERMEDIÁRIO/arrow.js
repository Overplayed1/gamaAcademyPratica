function soma(x, y) {
    return x + y;
}

const multiplicacao = function (x, y) {
    return x * y;
}

const dividir = (x, y) => {
    return x / y;
}

const Objeto = () => ({ nome: 'João Victor', sobrenome: 'Escudeiro'});

// console.log(soma(4,3));
// console.log(multiplicacao(5, 5));
// console.log(dividir(10,5));